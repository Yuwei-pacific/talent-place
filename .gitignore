# macOS
.DS_Store

# Node build output — tests import from lib/, but `npm test` runs `npm run build` first
engine/lib/

# Dependencies
node_modules/

# Python bytecode
__pycache__/
*.pyc

# Sync-export scratch: machine CSVs, run manifests, pre-write backups.
# These are regenerated per run and the backups can be large.
_machine/
Review-additions-*.csv

# Pre-write backups of the canonical CSV (git already tracks the file itself).
index/_backups/
.playwright-mcp/console-2026-09-17T09-13-16-548Z.log
/.playwright-mcp
